# Merchant app env. Copy to `apps/merchant/.env`.
# Merchant currently does not consume @cloud/auth, @cloud/db, or @cloud/cache,
# so only the public app name is required.

NEXT_PUBLIC_APP_NAME=Cloud Merchant
